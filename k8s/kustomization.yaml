apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: iu-agent
resources:
  - namespace.yaml
  - configmap.yaml
  - qdrant.yaml
  - pvc.yaml
  - agent.yaml
  - ingest-job.yaml
# Secrets are created from your .env file, see README (kubectl create secret generic iu-agent-secrets ...)
